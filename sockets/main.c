#include <stdio.h>
#include <stdlib.h>

int connect(char *ip, int port) {
    // Create a socket
    // Connect to the server using the provided IP and port
    // Return the socket file descriptor or -1 on failure
    // TODO: Implement
    return -1;
}

void bounce(int sock) {
    // Receive a string of length BUF = 256 from the server
    // Send the same string back to the server
    // Close the connection
    // TODO: Implement
}

int main() {
    return 0;
}
